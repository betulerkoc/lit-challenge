{
  "name": "lit-challenge",
  "version": "1.0.0",
  "description": "Employee Management System",
  "type": "module",
  "scripts": {
    "start": "web-dev-server --config web-dev-server.config.mjs --open --watch",
    "build": "tsc",
    "watch": "tsc --watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "lit": "^2.8.0"
  },
  "devDependencies": {
    "@web/dev-server": "^0.3.0",
    "@web/dev-server-esbuild": "^0.3.0",
    "@types/node": "^20.11.24",
    "typescript": "^5.3.3"
  }
}
